# 변경 기록

## 0.2.3 — 2026-08-09

0.2.2 에서 추가한 `windows-install` CI 가 첫 실행에서 바로 진짜 결함 두 종류를 잡았다.
CI 를 만든 보람이 있었다 — 실제 Windows 환경 없이는 못 찾았을 것들이다.

### 고침

- **`os.kill(pid, 0)` 이 Windows 에서 `OSError: [WinError 87]` 로 죽던 문제.** 죽은 잠금을
  치우는 로직이 "프로세스가 살아있나" 확인에 POSIX 전용 관용구를 썼다. Windows 엔 그
  신호 체계가 없다. 커널 API(`OpenProcess`)로 직접 확인하도록 바꿨다 — 새 의존성 없이
  (`ctypes` 는 표준 라이브러리). 접근 거부와 존재하지 않음을 구분해 "애매하면 살아있다고
  본다"는 안전 원칙을 그대로 지켰다.
- **한글 메시지를 출력하면 Windows 에서 `UnicodeEncodeError` 로 죽던 문제.** Windows 콘솔은
  로캘 코드페이지(cp1252 등)를 기본으로 쓴다. 이 도구는 한글로 말하므로, 이게 없으면
  `afterlimit scan`/`config` 등 거의 모든 명령이 Windows 에서 죽었다. `main()` 진입 시
  stdout/stderr 를 UTF-8 로 강제한다.
- 테스트 파일의 `write_text` 호출에도 같은 이유로 `encoding="utf-8"` 을 명시했다(파이썬은
  인코딩을 안 주면 플랫폼 로캘을 따른다 — 그게 Windows CI 에서 실패의 근본 원인이었다).
- 회귀 테스트 1개 추가(가짜 `ctypes` 로 Windows 분기를 macOS/Linux 에서도 검증, 총 82 통과).

## 0.2.2 — 2026-08-09

macOS 를 고친 뒤 "윈도우도 확실하냐"는 질문에 실제로 PowerShell 을 설치해 돌려보고 답했다.
`install.ps1` 은 **지금까지 한 번도 실제로 파싱된 적이 없었을 가능성**이 있었다.

### 고침

- **`install.ps1` 이 애초에 문법 오류였다.** `try { $pyOk = (python -c "..."; $LASTEXITCODE -eq 0) } catch {}`
  — PowerShell 의 `(...)` 는 파이프라인 하나만 감싼다. 세미콜론으로 여러 문장을 묶으려면
  `$(...)`(하위표현식)가 필요하다. `bash -n install.sh` 문법체크는 `.ps1` 을 아예 보지
  않아서 지금까지 못 잡았다. pwsh 파서로 실측 확인.
- **`.` (현재 디렉토리) 로 설치해 실행 위치에 따라 어긋나던 문제.** 저장소가 아닌 곳에서
  실행하면(더블클릭·절대경로 실행이 흔한 Windows 에서 흔한 상황) `pip install --user .`
  가 엉뚱한 걸 설치하거나 실패했다. `$PSScriptRoot`(스크립트 자신의 위치)로 바꿨다 —
  macOS 쪽 `REPO_DIR` 과 같은 고침.
- **pip 실패를 감지하지 못하던 문제.** 실패해도 스크립트가 멈추지 않아(PowerShell 은
  네이티브 명령의 0 아닌 종료코드로 자동으로 안 멈춘다) 계속 진행했다. `$LASTEXITCODE`
  를 직접 확인하게 했다.
- **전용 가상환경 폴백이 없던 문제.** `pip install --user` 가 실패하면 대안이 아예 없어
  Task Scheduler 에 실행 안 되는 작업을 등록했다. macOS 와 같은 전용 venv 폴백을 추가했다
  — 실행 파일이 그 venv 의 `python.exe` 를 절대경로로 직접 부른다.
- **설치 성공을 검증하지 않던 문제.** 이제 등록 전에 `--version` 을 설치 폴더 밖에서 실제로
  실행해 확인한다. 실패하면 등록하지 않고 죽는다.
- CI 에 `windows-install` 잡 추가 — 실제 Windows 러너에서 `install.ps1` 실행 → 검증 →
  Task Scheduler 등록 확인 → 재설치 → 제거까지 전부 실행 검증한다. 지금까지 Windows 경로는
  CI 에서 완전히 검증된 적이 없었다.

## 0.2.1 — 2026-08-09

`install.sh` 가 **실패했는데 성공했다고 말하고 있었다.** "git 하나만 설치하면 다른 사람 환경에서도
알아서 될까?" 라는 질문에 실제로 재현해서 답했다 — 아니었다.

### 고침

- **pipx 설치 실패를 감지하지 못하고 조용히 폴백하던 문제.** `pipx install --force` 가
  (uv 백엔드에서 흔한) "세션 밖에서 만든 venv 충돌"로 실패했는데, 실패 자체를 확인하지
  않고 다음 방법으로 넘어갔다. 이제 실패하면 지우고 새로 깐다 — 업그레이드 시 항상
  거치는 정상 경로로 만들었다.
- **오프라인 폴백이 실제로는 아무것도 설치하지 않던 문제.** pip 도 PEP 668(Homebrew Python)
  로 막히면 쓰던 폴백이, 설치 시점 `python3` 의 site-packages 에 `.pth` 파일을 심는
  방식이었다. macOS 는 `python3` 가 여러 개 흔한데(system·Homebrew·pyenv), 실행 시점
  `python3` 가 설치 시점과 다르면 `ModuleNotFoundError` 로 조용히 깨졌다 — **그러면서
  "완료했습니다"를 출력했다.** 실측: 레포 폴더 밖 깨끗한 셸에서 100% 재현됨.
  이제 이 설치 전용 가상환경을 만들고, 실행 파일이 그 venv 의 python 을 절대경로로
  직접 부른다 — 어떤 `python3` 가 PATH에 있든 어긋나지 않는다.
- **설치 성공을 검증하지 않고 그냥 성공 메시지를 냈던 문제.** 이제 설치 끝에 레포 폴더 밖의
  깨끗한 셸에서 `afterlimit --version` 을 실제로 실행해 확인한다. 안 되면 "완료했습니다"
  대신 실패로 죽는다.
- CI 에 `macos-install` 잡 추가 — `install.sh` 를 실제로 실행하고, 설치 폴더 밖에서
  검증하고, launchd 에이전트 등록을 확인하고, 재설치(업그레이드) 경로까지 같은 방식으로
  검증한다. 지금까지 Linux 설치 경로만 실제 실행 검증이 있었고 macOS 는 문법 체크뿐이었다.

## 0.2.0 — 2026-08-08

무인 재개를 하룻밤 돌려보고 나온 결함들을 고쳤다. 0.1.0 은 **재개가 안 되는 세션일수록
더 자주 두드리는** 구조였다. 실측: 재개 139건 중 137건이 헛돌았고 실제로 이어간 건 2건.

### 고침

- **실패를 기록하지 않아 무한 재시도하던 문제.** 재개했는데 한도가 그대로면 아무 기록도
  남기지 않아, 다음 사이클에 "재개한 적 없음"으로 보여 또 두드렸다. 쿨다운은 *성공한*
  재개에만 걸려 있었다. 이제 실패 횟수로 **1·2·4·8·16시간, 최대 24시간** 물러난다.
  이어가기에 성공하면 실패 기록을 지운다.

- **일을 하고 끝에 한도에 걸린 것을 실패로 세던 문제.** `hit_limit_again` 이 출력 텍스트에
  한도 문구가 있는지만 봤다. 그래서 9분을 일하고 마지막에 걸린 재개와 1초 만에 튕긴 재개를
  똑같이 실패로 셌다. 실측: 559초 동안 블로그 초안 5,576자를 만들고 커밋까지 한 세션이
  '한도 걸림'으로 기록됐고, 그렇게 쌓인 가짜 실패로 세션 145개가 최대 6시간 백오프에 갇혀
  재개가 실제로 멎었다. 이제 한도 안내 줄을 뺀 **산출량**으로 진척을 잰다
  (200자 이상이면 진척으로 본다).

- **지출 한도에 가짜 해제 시각을 만들던 문제.** 조직 월 지출 한도에는 해제 시각이 없는데
  `앵커 + 30분` 으로 지어냈다. 그 시각이 지나면 영원히 "풀렸다"로 판정된다.
  이제 `reset_at=None` 으로 두고 백오프가 속도를 정한다.
  ⚠️ 자동 재개 대상에서 **빼지는 않는다** — 빼면 한도가 풀렸는지 확인할 길이 사라진다.

- **계정 지출 한도를 세션마다 따로 배우던 문제.** 지출 한도는 계정 전체 조건이라 한 세션이
  튕기면 나머지도 전부 튕긴다. 세션 76개가 각자 부딪혀 하룻밤에 300번을 태웠다.
  이제 한 번 확인하면 전역으로 기록해 다 같이 쉰다. 재개에 성공하면 함께 푼다.

- **죽은 잠금이 남아 무인 재개가 통째로 멎던 문제.** 실행 도중 프로세스가 죽으면
  (스케줄러 재적재·재부팅·강제종료) `run.lock` 이 남아 이후 모든 사이클이
  "이미 실행 중입니다" 로 아무 일도 하지 않았다. 나이 제한(16분)만 있었다.
  이제 잠금 주인의 PID 생사를 확인한다. 확실히 죽었을 때만 치우고, 읽을 수 없거나
  남의 프로세스면 남긴다(PID 재사용 때문에 안전한 쪽으로).

### 나아진 것

- 로그에 **소요 시간과 산출량**을 남긴다. 이게 없으면 "바로 튕김"과 "한참 일하다 끝에 걸림"을
  구분할 수 없어, 실제로 진척된 작업까지 실패로 읽게 된다.
  - `이어감 · 559초 · 5576자 · 완료 · 끝에 한도 재도달`
  - `한도 재차단(2회) · 3초 · 12자 → 2시간 뒤 재시도`
- `scan` 이 지출 한도 세션에 해제 시각 대신 사유를 보여준다.
- 회귀 테스트 77개 (0.1.0 은 62개).

### 정리

- 빌드 산출물(`build/`, `*.egg-info/`)을 추적에서 빼고 `.gitignore` 에 넣었다.

### 알려진 한계

- **세션이 돌린 명령이 한도에 걸린 경우는 다루지 않는다.** 이 도구는 *세션 자신의*
  API 호출이 한도에 막힌 경우만 재개한다. 에이전트가 shell 로 실행한 명령이 한도에 걸리고
  에이전트가 그 사실을 사람에게 보고한 뒤 대기 중이라면, 마지막 메시지가 정상 응답이므로
  "막힌 세션"으로 잡히지 않는다. 화면에는 빨간 한도 메시지가 남지만 재개 대상이 아니다.

## 0.1.0

첫 공개.
